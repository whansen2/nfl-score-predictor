# Core dependencies
pandas==2.2.1
pyyaml==6.0.1
scikit-learn==1.4.1.post1

# Local development and testing
pytest==8.1.1
python-dotenv==1.0.0

# AWS Lambda runtime (required for S3 operations in container deployment)
boto3==1.34.84

# NFL stadium/weather data (with dependencies pinned to avoid numpy compilation)
nfl-stadiums==2.0.3
lukhed-basic-utils==1.4.7
